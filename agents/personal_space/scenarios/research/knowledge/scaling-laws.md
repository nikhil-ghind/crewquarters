# Overview

The main topics: how loss falls with model size, data, and compute. Key facts: doubling compute gives a predictable drop in loss. Important details: the paper fits a power law and reports the training cost of each run.
